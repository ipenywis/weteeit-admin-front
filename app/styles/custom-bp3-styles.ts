import { createGlobalStyle } from 'styled-components';

const Blueprint3CustomStyles = createGlobalStyle`
  /* Popover */
  .bp3-transition-container{
    margin-top: 10px !important;
  }
`;

export default Blueprint3CustomStyles;
